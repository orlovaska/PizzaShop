<UserControl x:Class="ShopWPFUI.Views.PlaceOrderView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ShopWPFUI.Views" xmlns:viewmodels="clr-namespace:ShopWPFUI.ViewModels" d:DataContext="{d:DesignInstance Type=viewmodels:PlaceOrderViewModel}"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <StackPanel
            Orientation="Vertical"
            ScrollViewer.HorizontalScrollBarVisibility="Disabled" >
            <Button x:Name="btn2Login" 
                        Grid.Row="2"
                        Grid.Column="1"
                        Command="{Binding PlaceOrderCommand}"
                        Content="Оформить заказ"
                        Style="{StaticResource BtnStyle}"
                        Margin="0,12,0,12" />
        </StackPanel>
            
    </Grid>
</UserControl>
